---
// The span of plugin API levels the current shell accepts, e.g. "3 through 12".
import { CURRENT_PLUGIN_API, OLDEST_SUPPORTED_PLUGIN_API } from '../data/plugin-api';
---

<span>{OLDEST_SUPPORTED_PLUGIN_API} through {CURRENT_PLUGIN_API}</span>
