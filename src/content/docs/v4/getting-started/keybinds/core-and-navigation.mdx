---
title: Core & Navigation
description: Essential commands for navigating and controlling the shell.
---

import IpcCommand from '../../../../../components/IpcCommand.astro';

These are the most common commands used for opening basic shell interfaces.

### Core Functions

| Function | Command | Description |
|----------|---------|-------------|
| **Launcher** | <IpcCommand prefix="ipc call" command="launcher toggle" /> | Open/close application launcher |
| **Control Center** | <IpcCommand prefix="ipc call" command="controlCenter toggle" /> | Toggle control center visibility |
| **Settings** | <IpcCommand prefix="ipc call" command="settings toggle" /> | Toggle settings (General tab) |
| **Settings Tab** | <IpcCommand prefix="ipc call" command="settings toggleTab $tab" /> | Toggle settings on a specific tab |
| **Open Settings** | <IpcCommand prefix="ipc call" command="settings open" /> | Open settings (General tab) |
| **Open Settings Tab** | <IpcCommand prefix="ipc call" command="settings openTab $tab" /> | Open settings on a specific tab |

:::note[Settings tab parameter]
`$tab` is a tab name (case-insensitive, hyphens/underscores ignored): `general`, `bar`, `dock`, `wallpaper`, `color-scheme`, `user-interface`, `control-center`, `desktop-widgets`, `launcher`, `notifications`, `osd`, `lock-screen`, `session-menu`, `audio`, `display`, `network`, `location`, `system-monitor`, `plugins`, `hooks`, `about`.

Append `/$subtab` to navigate to a specific subtab index, e.g. `settings openTab bar/2`.
:::

### Quick Access

| Function | Command | Description |
|----------|---------|-------------|
| **Clipboard** | <IpcCommand prefix="ipc call" command="launcher clipboard" /> | Access clipboard history |
| **Emoji** | <IpcCommand prefix="ipc call" command="launcher emoji" /> | Quick emoji selector access |
| **Command** | <IpcCommand prefix="ipc call" command="launcher command" /> | Quick command runner access |
| **Windows** | <IpcCommand prefix="ipc call" command="launcher windows" /> | Window switcher access |
| **Settings Search** | <IpcCommand prefix="ipc call" command="launcher settings" /> | Settings search access |
| **Calendar** | <IpcCommand prefix="ipc call" command="calendar toggle" /> | Open/close Calendar window |
| **Session** | <IpcCommand prefix="ipc call" command="sessionMenu toggle" /> | Logout, reboot, shutdown... |
| **Lock** | <IpcCommand prefix="ipc call" command="lockScreen lock" /> | Launch the lock screen |
| **Lock & Suspend** | <IpcCommand prefix="ipc call" command="sessionMenu lockAndSuspend" /> | Lock screen and suspend system |
| **System Monitor** | <IpcCommand prefix="ipc call" command="systemMonitor toggle" /> | Open/close system monitor |
