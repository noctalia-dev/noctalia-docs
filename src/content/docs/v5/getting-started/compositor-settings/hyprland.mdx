---
title: Hyprland
description: Configure Hyprland for the best experience with Noctalia.
sidebar:
  order: 2
---

import ImageLightbox from '../../../../../components/ImageLightbox.astro';
import blur1 from '../../../../../assets/hyprland-blur/hyprland-blur-1.png';

Add the following settings to your existing `hl.config({})` block, usually located in your Hyprland Lua configuration file (usually located at `~/.config/hypr/hyprland.lua`).

These settings are for general window appearance and functionality.

```lua
hl.config({
  general = {
    gaps_in = 5,
    gaps_out = 10,
  },

  decoration = {
    rounding = 20,
    rounding_power = 2,

    shadow = {
      enabled = true,
      range = 4,
      render_power = 3,
      color = 0xee1a1a1a,
    },

    blur = {
      enabled = true,
      size = 3,
      passes = 2,
      vibrancy = 0.1696,
    },
  },
})
```

## Persistent Workspaces

For the best workspace indicator behavior, make your regular Hyprland workspaces persistent. This keeps empty workspaces visible in Noctalia instead of only showing workspaces that currently contain windows.

Update the monitor name and workspace list for your setup. You can find more syntax and examples in the official [Workspace Rules](https://wiki.hypr.land/Configuring/Basics/Workspace-Rules/) documentation.

```lua
hl.workspace_rule({ workspace = "1", monitor = "DP-1", persistent = true })
hl.workspace_rule({ workspace = "2", monitor = "DP-1", persistent = true })
hl.workspace_rule({ workspace = "3", monitor = "DP-1", persistent = true })
hl.workspace_rule({ workspace = "4", monitor = "DP-1", persistent = true })
hl.workspace_rule({ workspace = "5", monitor = "DP-1", persistent = true })
```

## Blur

Next, you can enable blur for Noctalia's bar, panels, dock, and notifications.

<ImageLightbox src={blur1} alt="Blurred Noctalia surfaces" />

```lua
hl.layer_rule({
  name = "noctalia",
  match = {
    namespace = "^noctalia-(bar-.+|notification|dock|panel)$",
  },
  ignore_alpha = 0.5,
  blur = true,
  blur_popups = true,
})
```
