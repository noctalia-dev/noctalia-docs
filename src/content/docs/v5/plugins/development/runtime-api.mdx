---
title: Runtime API
description: Use noctalia.* host capabilities and share state across plugin entries.
sidebar:
  label: Runtime API
  order: 5
---

:::caution[Experimental — under heavy development]
The plugin system is under **heavy development** and the manifest format, APIs, and behavior are subject to breaking
changes at any time.
:::

## `noctalia.*` — host capabilities

| Group | Functions |
|-------|-----------|
| Runtime | `setUpdateInterval(ms)`, `log(msg)`, `isDarkMode()`, `focusedOutputName()`, `wallpaperDirectory()`, `getConfig(key)` |
| Outputs | `outputs()` → array of `{ name, description, width, height, x, y, scale, focused }`; `setWallpaperEnabled(connector, enabled)` |
| Wallpaper | `setWallpaper(path)` / `setWallpaper(connector, path)` — apply and persist a wallpaper image |
| Panels | `togglePanel("author/plugin:panel")` — open/close one of this plugin's panels |
| Time | `formatTime(pattern [, unixSeconds])` |
| Notify | `notify(title, body)`, `notifyError(title, body)` |
| Subprocess | `runAsync(cmd [, cb])`, `runStream(cmd, onLine)`, `runInTerminal(cmd)`, `commandExists(name)`, `processMatches(cb, ...needles)`, `flatpakAppInstalled(id)`, `portalAvailable()`, `getenv(name)`, `expandPath(path)` |
| Clipboard | `copyToClipboard(text [, mime])` |
| Filesystem | `readFile(path)`, `writeFile(path, content)`, `fileExists(path)`, `listDir(path)`, `pluginDir()` — relative paths resolve against the plugin's own directory |
| i18n | `tr(key [, subst])`, `trp(key, count [, subst])` — against `translations/<lang>.json` |
| HTTP | `http({ url = … }, cb)`, `download(url, dest, cb)` — async; the callback fires when the request lands |
| JSON | `json.decode(str)` → value, or `nil, err` on malformed input; `json.encode(value [, pretty])` → string |
| String | `string.trim(s)` → whitespace-trimmed copy; `string.urlEncode(s)` / `string.urlDecode(s)` → percent-encode/decode for URL query components |

`formatTime` uses the same date-format tokens as Noctalia clock and filename settings. `unixSeconds` is optional; when
omitted, it formats the current local time. `%s` expands to Unix epoch seconds.

`wallpaperDirectory()` returns the resolved global wallpaper folder for the current theme mode (light/dark), or `nil`
when unset. Paths are already expanded — use them for download targets or to mirror built-in wallpaper behavior.

`outputs()` returns the connected outputs (logical geometry, DRM connector `name`, and which one is `focused`) —
the connector name matches what compositors and tools like `mpvpaper` expect. A `[[service]]` may define a global
`onOutputsChanged()` function, called whenever the output set or geometry changes, to reconcile per-output work.
`setWallpaperEnabled(connector, false)` tears down Noctalia's own wallpaper surface on that output (so an external
source can draw there) and `true` restores it; the suppression is runtime-only and clears on restart.

`setWallpaper(path)` applies and persists an image as the wallpaper, exactly like the wallpaper panel — with one
argument it targets every output, or pass `setWallpaper(connector, path)` for a single output. `togglePanel(id)` opens
or closes a panel by its full entry id (`author/plugin:panel`). Prefer these over shelling out to the `noctalia` CLI:
they run in-process, with no subprocess or IPC round-trip.

`json.*` and `string.*` are synchronous pure transforms (no callback), unlike HTTP and filesystem. Prefer
`string.urlEncode` when building query strings for `http`/`download` rather than hand-rolling percent-encoding.
Filesystem and HTTP run safely off the UI thread; results arrive via callbacks. `runAsync` runs a command to completion
and calls its callback once with the output; `runStream` runs a long-lived command and calls `onLine(line)` for each
output line as it arrives — the process is terminated automatically when the script reloads or the widget is removed.

## Sharing state across entries

Entries are isolated VMs — they don't share Lua memory. Instead they exchange **plain values** through a per-plugin
state channel: `noctalia.state.set(key, value)`, `noctalia.state.get(key)`, and `noctalia.state.watch(key, fn)` (fires
when the value changes). Values are **copied** across entries, so they must be plain data — strings, numbers, booleans,
and tables of those (not functions). A typical pattern is a `[[service]]` publishing data that the widget and shortcut
watch.

```lua
-- ticker.luau (service)
local n = 0
noctalia.setUpdateInterval(1000)
function update()
  n = n + 1
  noctalia.state.set("count", n)
end

-- widget.luau
noctalia.state.watch("count", function(value)
  barWidget.setText(tostring(value))
end)
```
